{% extends 'App.html.twig' %}

{% block Content %}

    <div class="heading h1">
        <div class="row align-items-center">
            <div class="col-md-8">
                <h1>Cronjobs</h1>
            </div>
            <div class="col-md-4">

                {#
                <div class="gap-2 d-md-flex justify-content-md-end">
                    <a data-modal data-title="Text hinzufügen" class="btn btn-primary" href="{{ controller.getUri('Admin/Texts', 'ajaxModalCompose') }}"><i class="fa fa-plus"></i>Text hinzufügen</a>
                </div>
                #}

            </div>
        </div>
    </div>

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ controller.getUri('Admin', 'index') }}">Admin</a></li>
            <li class="breadcrumb-item"><a href="{{ controller.getUri('Admin/Maintenance', 'index') }}">Wartung</a></li>
            <li class="breadcrumb-item active" aria-current="page">Cronjobs</li>
        </ol>
    </nav>

    <div class="card">
        <div class="card-body">
            <div class="row align-items-center">
                <div class="col-10">
                    <code>{{ phpPath | default('/opt/php-8.0/bin/php') }} {{ binPath }}cron-daily.php</code>
                </div>
                <div class="col-2 text-end fs-2">
                    <a data-bs-toggle="tooltip" data-bs-title="Cron ausführen" class="ajax" href="{{ controller.getActionUri('ajaxPlay', { cron: "daily" }) }}"><i class="far fa-play"></i></a>
                </div>
            </div>
        </div>
    </div>

    <div class="console daily d-none" style="background: #000; padding: 20px; border-radius: 10px; color: #FFF; font-family: Courier;"></div>


{% endblock %}